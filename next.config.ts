import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['github.com'], // Añade aquí cualquier otro dominio que necesites
  },
};

export default nextConfig;
